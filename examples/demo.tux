import math
import text

function fact(n)
    if n <= 1 then
        return 1
    end

    return n * fact(n - 1)
end

local values = [3, 6, 9]
local meta = {"lang": "tux", "fast": true}
print(values[0], values[2], text.upper(meta["lang"]), fact(6), math.sqrt(144))
